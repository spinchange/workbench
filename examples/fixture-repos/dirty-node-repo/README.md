# Dirty Node Repo

This fixture exists to exercise degraded proof-round paths.

- it has a real `package.json`
- it has a real `test` script
- it is intended to simulate a repo that is not clean during a proof round
