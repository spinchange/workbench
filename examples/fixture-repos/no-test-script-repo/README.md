# No Test Script Repo

This fixture exists to exercise the explanatory path in `workbench proof`.

- it has a real `package.json`
- it intentionally has no `test` script
- it should stay clean in git
